services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: spademoney
      POSTGRES_USER: spademoney
      POSTGRES_PASSWORD: spademoney
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
